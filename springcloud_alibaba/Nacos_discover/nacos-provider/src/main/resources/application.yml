server:
  port: 8081

spring:
  application:
    name: nacos-provider

  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 #注册中心地址

management:
  endpoints:
    web:
      exposure:
        include: '*'
